# 就是你的官方包存放以及自动下载的刷机包的路径,默认为officalzip文件夹
# 需要处理的刷机包将从你定义的路径内读取
officalzip=officialzip

# 就是制作好的的刷机包的存放路径,默认为outputzip文件夹
outputzip=outputzip

# 仅解包吗,1为真，其他为否
only=0

# 保留官方包吗,1为真，其他为否
keep=0

# 制作线刷包（只针对小米11等虚拟ab分区机型）吗？1为制作线刷包,3为同时制作卡刷和线刷包，其他为制作卡刷包
fastbootflash=0